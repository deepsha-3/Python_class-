matrix=[
    [1, 2, 3],    # this is first row (index 0)
    [4, 5, 6],    # this is second row (index 1)
    [7, 8, 9]     # this is third row (index 2)
]


# for row in matrix:
#     print(row)

for row in matrix:
    for item in row:
        print(item)